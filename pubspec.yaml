name: counter_app_reaction_game
description: 'Counter App Reaction Game - #FlutterCounterChallenge2020'

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.7.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  confetti: ^0.5.4+1
  cupertino_icons: ^1.0.0
  stop_watch_timer: ^0.6.0+1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
